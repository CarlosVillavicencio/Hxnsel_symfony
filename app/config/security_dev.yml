# To get started with security, check out the documentation:
# http://symfony.com/doc/current/security.html
security:
    encoders:
        FOS\UserBundle\Model\UserInterface: bcrypt
    role_hierarchy:
            ROLE_ADMIN:       ROLE_USER
            ROLE_SUPER_ADMIN: ROLE_ADMIN
#        LoginDevBundle\Entity\Accesousuario:
#            algorithm: md5
#            encode_as_base64: false
#            iterations: 0
        #Symfony\Component\Security\Core\User\User: plaintext
    # http://symfony.com/doc/current/security.html#b-configuring-how-users-are-loaded
    providers:
        fos_userbundle:
            id: fos_user.user_provider.username
        #in_memory:
        #    memory:
        #        users:
        #            admin:
        #                password: 1234
        #                roles: 'ROLE_ADMIN'
#        our_db_provider:
#            entity:
#                class: LoginDevBundle:Accesousuario
#                property: username
#        database_users:
#            entity: {class: LoginDevBundle:Accesousuario, property: username}

    firewalls:
        # disables authentication for assets and the profiler, adapt it according to your needs
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        #admin:
        #    anonymous: ~
        #    pattern: ^/proyectos
        #usuarios:
        #    #pattern: ^/
        #    anonymous: ~
        #    provider: our_db_provider
        #    form_login:
        #        login_path: login
        #        check_path: login
        main:
#            anonymous: ~
            pattern:    ^/
            form_login:
                provider: fos_userbundle
                csrf_token_generator: security.csrf.token_manager
                # if you are using Symfony < 2.8, use the following config instead:
                # csrf_provider: form.csrf_provider
#                csrf_provider: security.csrf.token_manager # Use form.csrf_provider instead for Symfony <2.4
            logout:       true
            anonymous:    true
#            http_basic: ~
#            guard:
#                authenticators:
#                    - app.form_login_authenticator
            #provider: our_db_provider
            #form_login:
            #    login_path: loginV1_dev
            #    check_path: security_login_checkV1_dev
            #    default_target_path: homepage
            #    always_use_default_target_path: false
            #    use_referer: true
            #    #use_forward: false
            #    failure_path: loginV1_dev
#            logout:
#                path: /logout
#                target: loginV1_dev
    access_control:
        - { path: ^/inicio/, roles: ROLE_USER }
        - { path: ^/group/, roles: ROLE_USER }
        # require ROLE_ADMIN for /admin*
        - { path: ^/login$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/resetting, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/admin/, role: ROLE_ADMIN }


        - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/dashboard, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        #- { path: ^/proyectos, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/, roles: ROLE_UC_CALIFICADOR }
        - { path: ^/, roles: ROLE_ADMIN }
            # activate different ways to authenticate

            # http_basic: ~
            # http://symfony.com/doc/current/security.html#a-configuring-how-your-users-will-authenticate

            # form_login: ~
            # http://symfony.com/doc/current/cookbook/security/form_login_setup.html
